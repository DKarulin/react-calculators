const initialState = {
    nameСurrencySale: 'RUB',
    nameСurrencyBuy: 'USD',
}

export const currencyReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}